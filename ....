loadstring(game:HttpGet(("https://raw.githubusercontent.com/Luarmor123/community-Y-HUB/refs/heads/main/Gui")))()

local Window = redzlib:MakeWindow({
  Title = "S2 hub",
  SubTitle = "by : S2 team",
  LoadText = "redz Hub",
  Flags = "S2 hub"
})

local Notify = Library:MakeNotify({
  Title = "loading",
  Text = "S2 hub",
  Time = 5
}

local Tab = Window:MakeTab({Name = "سكربتات", Icon = "سكربتات"})

local Button = Tab:AddButton({
  Name = "ss",
  Callback = function()
    print(nothing)
  end
})
